/**
 * src/core — game loop, fixed-timestep ticker, event bus (DESIGN.md §1).
 *
 * Import the barrel (`./core`) or the individual modules; both work.
 */

export * from './eventBus';
export * from './ticker';
